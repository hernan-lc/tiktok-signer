{
  "name": "ttl-live-bun-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bun + HTML console-log check for the direct TikTok LIVE SDK. uniqueId comes from the page query string.",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --hot run server.ts"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.1",
    "ws": "^8.18.0"
  }
}
