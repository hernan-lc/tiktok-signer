{
  "name": "ttl-live",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TikTok LIVE events in Node, with no sign server, no native module and no browser.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src", "schema", "scripts", "vendor", "README.md"],
  "engines": { "node": ">=22" },
  "scripts": {
    "clean": "node -e \"const fs=require('node:fs'); for (const path of ['dist','.test-dist','.example-dist']) fs.rmSync(path,{recursive:true,force:true})\"",
    "prebuild": "npm run clean",
    "build": "tsc -p tsconfig.json",
    "typecheck": "npm run build && tsc -p tsconfig.test.json --noEmit && tsc -p tsconfig.example.json --noEmit",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test .test-dist/*.test.js",
    "prepack": "npm run check",
    "sync-bootstrap": "node ../../scripts/headless/tools/build-bootstrap.mjs vendor/bootstrap.js",
    "listen": "npm run build && tsc -p tsconfig.example.json && node .example-dist/listen.js",
    "proto:generate": "cd ../.. && buf generate --template buf.gen.yaml",
    "proto:check": "npm run proto:generate && git diff --exit-code -- src/gen/webcast",
    "schema:generate": "node scripts/generate-json-types.mjs",
    "schema:check": "npm run schema:generate && git diff --exit-code -- src/gen/json",
    "generate": "npm run proto:generate && npm run schema:generate",
    "check": "npm run proto:check && npm run schema:check && npm run typecheck && npm test"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.1"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.72.0",
    "@bufbuild/protoc-gen-es": "^2.14.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
